{% extends 'base.html.twig' %}

{% block title %}Signification{% endblock %}

{% block body %}
    <h1>Signification</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ signification.id }}</td>
            </tr>
            <tr>
                <th>Mots</th>
                <td>{{ signification.mots }}</td>
            </tr>
            <tr>
                <th>Definition</th>
                <td>{{ signification.definition }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_signification_index') }}">back to list</a>

    <a href="{{ path('app_signification_edit', {'id': signification.id}) }}">edit</a>

    {{ include('signification/_delete_form.html.twig') }}
{% endblock %}
