<form method="post" action="{{ path('app_specialisation_delete', {'id': specialisation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specialisation.id) }}">
    <button class="btn">Delete</button>
</form>
