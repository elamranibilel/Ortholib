{% extends 'base.html.twig' %}

{% block title %}Specialisation index{% endblock %}

{% block body %}
    <h1>Specialisation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Label</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for specialisation in specialisations %}
            <tr>
                <td>{{ specialisation.id }}</td>
                <td>{{ specialisation.label }}</td>
                <td>
                    <a href="{{ path('app_specialisation_show', {'id': specialisation.id}) }}">show</a>
                    <a href="{{ path('app_specialisation_edit', {'id': specialisation.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_specialisation_new') }}">Create new</a>
{% endblock %}
