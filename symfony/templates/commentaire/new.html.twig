{% extends 'base.html.twig' %} {% form_theme form with
['@FOSCKEditor/Form/ckeditor_widget.html.twig', 'bootstrap_4_layout.html.twig']
%} {% block body %}
<div class="container">
  <hr />
  {{ form_start(form) }}
  {{ form_widget(form) }}
  <br />
  <button type="submit" class="btn btn-success">Enregistrer !</button>
  <a href="{{ path('app_commentaire') }}" class="btn btn-danger"> Retour </a>
  {{ form_end(form) }}
</div>

{% endblock %}
