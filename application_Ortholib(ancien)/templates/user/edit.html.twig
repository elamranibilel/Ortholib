{% extends 'base.html.twig' %}
{% form_theme formCreateUtilisateur 'bootstrap_4_layout.html.twig' %}
{% block body %}
<div class="container">
    <hr>
    {{ form_start(formCreateUtilisateur) }}
    {{ form_widget(formCreateUtilisateur) }}
    <button type="submit" class="btn btn-success">
        Register !
    </button>
    <a href="{{ path('app_user') }}" class="btn btn-danger">
        Retour
    </a>
    {{ form_end(formCreateUtilisateur) }}
    {% endblock %}